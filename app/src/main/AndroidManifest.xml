<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.artshell.arch.mvp"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- =============== arch_mvp begin =============== -->
        <activity
            android:name=".manual_impl.ProgressActivity"
            android:description="@string/arch_mvp"
            android:label="ProgressActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".annotation_impl.MasterActivity"
            android:description="@string/arch_mvp"
            android:label="MasterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".reactive_stream_impl.ReactiveActivity"
            android:description="@string/arch_mvp"
            android:label="ReactiveActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".live_data_impl.LiveDataActivity"
            android:description="@string/arch_mvp"
            android:label="LiveDataActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".dialog_fragment_impl.FellowshipActivity"
            android:description="@string/arch_mvp"
            android:label="FellowshipActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".presenter_lifecycle.LifecycleActivity"
            android:description="@string/arch_mvp"
            android:label="LifecycleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".fragment_lifecycle_state.FragmentLifecycleStateActivity"
            android:description="@string/arch_mvp"
            android:label="FragmentLifecycleStateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".lazyload.LoadingContainerActivity"
            android:description="@string/arch_mvp"
            android:label="LoadingContainerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- =============== end =============== -->
    </application>

</manifest>